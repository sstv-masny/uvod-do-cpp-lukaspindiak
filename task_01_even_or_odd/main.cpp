
#include <iostream>
int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    long long n;

    std::cout << "Nacitaj cislo";

    std::cin >> n;

    if (!(n))
        return 0;

    return 0;
}
